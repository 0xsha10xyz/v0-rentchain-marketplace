diff --git a/node_modules/x402-solana/dist/client/index.js b/node_modules/x402-solana/dist/client/index.js
index 917ab69..ca67c15 100644
--- a/node_modules/x402-solana/dist/client/index.js
+++ b/node_modules/x402-solana/dist/client/index.js
@@ -54,7 +54,7 @@ function getDefaultRpcUrl(network) {
   }
   return "https://api.devnet.solana.com";
 }
-var DEFAULT_COMPUTE_UNIT_LIMIT = 2e4;
+var DEFAULT_COMPUTE_UNIT_LIMIT = 2e5;
 var DEFAULT_COMPUTE_UNIT_PRICE_MICROLAMPORTS = 1;
 async function createSolanaPaymentTransaction(wallet, paymentRequirements, rpcUrl) {
   const connection = new web3_js.Connection(rpcUrl, "confirmed");
@@ -115,8 +115,13 @@ async function createSolanaPaymentTransaction(wallet, paymentRequirements, rpcUr
     "confirmed"
   );
   if (!destAtaInfo) {
-    throw new Error(
-      `Destination does not have an Associated Token Account for ${paymentRequirements.asset}. The receiver must create their token account before receiving payments.`
+    instructions.push(
+      splToken.createAssociatedTokenAccountIdempotentInstruction(
+        userPubkey,
+        destination,
+        mintPubkey,
+        programId
+      )
     );
   }
   const amountStr = paymentRequirements.amount || paymentRequirements.maxAmountRequired;
diff --git a/node_modules/x402-solana/dist/client/index.mjs b/node_modules/x402-solana/dist/client/index.mjs
index aba8da8..ea881a7 100644
--- a/node_modules/x402-solana/dist/client/index.mjs
+++ b/node_modules/x402-solana/dist/client/index.mjs
@@ -1,6 +1,6 @@
 import { safeBase64Decode } from '@payai/x402/utils';
 import { Connection, PublicKey, ComputeBudgetProgram, TransactionMessage, VersionedTransaction } from '@solana/web3.js';
-import { TOKEN_2022_PROGRAM_ID, TOKEN_PROGRAM_ID, getMint, getAssociatedTokenAddress, createTransferCheckedInstruction } from '@solana/spl-token';
+import { TOKEN_2022_PROGRAM_ID, TOKEN_PROGRAM_ID, getMint, getAssociatedTokenAddress, createTransferCheckedInstruction, createAssociatedTokenAccountIdempotentInstruction } from '@solana/spl-token';
 
 // src/types/x402-protocol.ts
 var SOLANA_MAINNET_CAIP2 = "solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp";
@@ -52,7 +52,7 @@ function getDefaultRpcUrl(network) {
   }
   return "https://api.devnet.solana.com";
 }
-var DEFAULT_COMPUTE_UNIT_LIMIT = 2e4;
+var DEFAULT_COMPUTE_UNIT_LIMIT = 2e5;
 var DEFAULT_COMPUTE_UNIT_PRICE_MICROLAMPORTS = 1;
 async function createSolanaPaymentTransaction(wallet, paymentRequirements, rpcUrl) {
   const connection = new Connection(rpcUrl, "confirmed");
@@ -113,8 +113,13 @@ async function createSolanaPaymentTransaction(wallet, paymentRequirements, rpcUr
     "confirmed"
   );
   if (!destAtaInfo) {
-    throw new Error(
-      `Destination does not have an Associated Token Account for ${paymentRequirements.asset}. The receiver must create their token account before receiving payments.`
+    instructions.push(
+      createAssociatedTokenAccountIdempotentInstruction(
+        userPubkey,
+        destination,
+        mintPubkey,
+        programId
+      )
     );
   }
   const amountStr = paymentRequirements.amount || paymentRequirements.maxAmountRequired;
